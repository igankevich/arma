sudo: required
dist: trusty
language: cpp
services:
  - docker
script:
  - docker run -v $PWD:/mnt docker.io/fedora sh -c 'cd /mnt; ./deploy/cibuild'
after_success:
  - "./deploy/cideploy"
branches:
  only:
  - master
