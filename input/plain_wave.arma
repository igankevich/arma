out_grid = (200,40,40)

model = plain_wave
plain_wave = {
	func = cos
	amplitudes = [2]
	wavenumbers = [0.5]
	phases = [2]
	velocity = 0.5
}
verification = manual

velocity_field = linear {
	wnmax = (0.5,0.5)
	depth = 10
	domain = from (10,-5) to (10,2) npoints (1,10)
}
